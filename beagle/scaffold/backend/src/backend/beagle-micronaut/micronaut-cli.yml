applicationType: default
defaultPackage: ${package_name}
testFramework: junit
sourceLanguage: kotlin
buildTool: maven
features:
  [
    annotation-api,
    app-name,
    docker,
    file-watch,
    http-client,
    junit,
    kotlin,
    kotlin-application,
    logback,
    maven,
    netty-server,
    readme,
    shade,
    yaml,
  ]
